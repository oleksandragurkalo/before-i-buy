VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only (never prefix with VITE_) — used by /api/delete-account.js.
# Set this in Vercel's dashboard for production/preview too, not just locally.
SUPABASE_SERVICE_ROLE_KEY=
